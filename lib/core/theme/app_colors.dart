import 'package:flutter/material.dart';

abstract final class AppColors {
  static const seedColor = Color(0xFF97CE4C);

  // Status indicator colors
  static const statusAlive = Colors.green;
  static const statusDead = Colors.red;
  static const statusUnknown = Colors.grey;

  // Accent
  static const favoriteActive = Colors.amber;
}
